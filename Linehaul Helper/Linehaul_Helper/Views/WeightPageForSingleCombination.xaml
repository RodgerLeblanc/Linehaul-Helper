<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Linehaul_Helper.CustomControls"
             x:Class="Linehaul_Helper.Views.WeightPageForSingleCombination"
             Title="WeightPageForSingleCombination (Unlocalized)"
             BackgroundColor="{StaticResource DicomOrange}">

    <Grid
        RowSpacing="{StaticResource GeneralSpacing}" 
        ColumnSpacing="{StaticResource GeneralSpacing}" 
        Padding="{StaticResource GeneralPadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Blue background, implicit style -->
        <BoxView Grid.Row="0"/>

        <controls:TruckSingleCombination
            Grid.Row="0"
            HorizontalOptions="Center"
            VerticalOptions="CenterAndExpand"
            NumberOfAxles="{Binding NumberOfAxles}"
            ImageSource="{Binding ImageSource}"/>

    </Grid>
</ContentPage>